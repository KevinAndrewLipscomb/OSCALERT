create user if not exists 'ki-oscalert-x'@'localhost' identified by '~ki-oscalert-x-password~';
grant  select
 , insert
 , update
 , delete
 , execute
 , create
 , alter
 , create routine
 , alter routine
  on oscardotnetdb_x.*
  to 'ki-oscalert-x'@'localhost'
;
--
-- Must grant PROCESS priv to primary user to include SHOW ENGINE INNODB
-- STATUS output in MySQL-related exception reports.
--
;
grant process
  on *.*
  to 'ki-oscalert-x'@'localhost'